# T09 verification — Executor, guards, retries and Settler in dry run; Trades page

Environment: Linux x86_64 (running as root), Node.js v22.22.2, npm 10.9.7, Playwright 1.63 with the pre-installed
Chromium, 2026-09-26. Everything runs locally: Kalshi is answered by the `msw` stand-in (unit tests) and by the
fixture-backed fake Kalshi server (e2e); the replay uses `test/fixtures/replay/nhl-sample.jsonl`. No Kalshi or NHL
host is contacted.

Automated checks: `cd kalshi-trader/app && npm run verify:T09`.

## Results

| # | Acceptance item | How | Result |
| --- | --- | --- | --- |
| 1 | Pricing values (fees, limit price incl. the $0.005 grid, contracts, realized / unrealized P&L) | `test/unit/pricing.test.ts` (every value of the item, plus sub-cent grids, multipliers, stake clamp) | PASS |
| 2 | Order of operations: attempt insert before market / orderbook requests; orderbook throws → attempt `error`, trade `waiting`, error logged, nothing thrown | `test/unit/core/executor.test.ts` › order of operations (msw handler records the `trade_attempts` rows at the moment each request arrives; `HttpResponse.error()` for the orderbook) | PASS |
| 3 | Guards with the named reason and no bankroll change (market_closed … too_small) | executor.test.ts › guards (8 cases; bankroll and `bankroll_snapshots` unchanged) and `test/unit/core/guards.test.ts` (order, classes) | PASS |
| 4 | Retry: 0.98 → waiting, 0.96 → filled at 0.97 with two attempts; window end → `skipped/price/window_expired`; lead drops → no attempt | executor.test.ts › retry (engine + executor on a fake tracker; request log shows no HTTP on the lead-drop and window-end ticks) | PASS |
| 5 | Happy path: stake 2 000 000, limit 9400, 200 cc, cost 1 880 000, fee 7 900, bankroll 98 112 100; `maxStakeUsd` 1 → 1 contract; 1 offered → 1 contract | executor.test.ts › happy path (also audit order and `mode` on every row) | PASS |
| 6 | Settler: won / lost / void with payout, P&L, bankroll and one snapshot; open market re-checked next minute; historical endpoint past the cutoff | `test/unit/core/settler.test.ts` (fake timers, msw; plus a 404 fallback) | PASS |
| 7 | Two strategies on one game in the same tick: both debits, sequentially | executor.test.ts › two strategies (snapshots 98 112 100 then 96 224 200) | PASS |
| 8 | Restart: pending dry-run attempt 10 min old → `unfilled/restart`; window closed → trade `skipped`, open → `waiting` | executor.test.ts › restart recovery | PASS |
| 9 | Global kill switch → zero HTTP from executor and settler, states unchanged; off → settlement catches up | settler.test.ts › global kill switch (msw `request:start` count unchanged over 5 min of ticks and settler runs) | PASS |
| 10 | End-to-end replay → one trade, `signalled → pending → filled → settled_*` in audit order with `mode='dry_run'`, bankroll updated; Trades page row with `DRY RUN`, snapshot and attempts | `test/unit/trades/pipeline.test.ts` › end-to-end (real app, `POST /api/dev/replay`, settler run, Trades API) and `test/e2e/trades.spec.ts` (replay at 100× into the e2e server, page checks) | PASS |
| 11 | Configured `live` + `allow_live_orders=false` → dry-run fill with `LIVE → DRY RUN (add-on lock)`; lock open, global dry run off, live → `hard_skip/live_not_implemented` | executor.test.ts › modes; badge text checked in trades.spec.ts | PASS |
| 12 | e2e: CSV header with the four mode columns, one line per visible trade; bankroll reset needs re-auth, restores the initial value, audit row + `reset` snapshot | `test/e2e/trades.spec.ts` at 1280 px and 390 px (download parsed; session aged so the prompt must appear; DB checked; no CSP violations, no sideways scroll) | PASS |

`npm run lint`, `npm run typecheck`, `npm test` (440 passed, 1 skipped as root: the T02 `chmod 555` test),
`npm run e2e` (30 passed) and `npm audit --audit-level=high` (exit 0) pass.

`docker compose up --build` could not be run in this session (no Docker daemon available; in T08 the egress proxy
also refused the pinned base image). The `Image` workflow (amd64 compose + arm64 build) covers it on the pushed
branch.

## `npm run verify:T09` output

```
PASS   Pricing … — 15 tests passed
PASS   Order of operations … — 2 tests passed, 21 skipped
PASS   Guards with no bankroll change … — 8 tests passed, 15 skipped; guard table: 5 tests passed
PASS   Retry … — 3 tests passed, 20 skipped
PASS   Happy path … — 4 tests passed, 19 skipped
PASS   Settler (fake timers, msw) … — 7 tests passed
PASS   Two strategies on one game fill in the same tick … — 1 tests passed, 22 skipped
PASS   Restart … — 2 tests passed, 21 skipped
PASS   Global kill switch … — 1 tests passed, 6 skipped
PASS   End-to-end replay … — 1 tests passed, 3 skipped
PASS   Configured live + allow_live_orders=false … — 4 tests passed, 19 skipped
PASS   (extra) Trades API filters, bankroll reset step-up and fee precision setting — 3 tests passed, 1 skipped
PASS   e2e: Trades page … — 2 e2e tests passed (1280 px + 390 px)

13 of 13 items not failing.
```
