# T06 verification — Kalshi API client, network gate, market discovery

Environment: Linux x86_64 (running as root), Node.js v22.22.2, npm 10.9.7, msw 2.15, Playwright 1.63 with the
pre-installed Chromium, 2026-09-26. The session's egress policy blocks every Kalshi host (`external-api*.kalshi.*`,
`api.elections.kalshi.com`, `docs.kalshi.com`): every check runs on the hand-written fixtures in
`kalshi-trader/app/test/fixtures/kalshi/`, served by `msw` (unit tests) or `test/e2e/fake-kalshi.ts` (e2e, smoke).

Automated checks: `cd kalshi-trader/app && npm run verify:T06`.

## Results

| # | Acceptance item | How | Result |
| --- | --- | --- | --- |
| 1 | Signing string and RSA-PSS verification; query excluded | `test/unit/kalshi/client.test.ts` › signing (per-run 2048-bit fixture key; `crypto.verify` with `RSA_PKCS1_PSS_PADDING` / `RSA_PSS_SALTLEN_DIGEST`; also the headers of a real request with a query string) | PASS |
| 2 | Every method has an msw test with typed integers; NO bid `0.0700` × `50.00` → `{9300, 5000}`; unknown field passes; missing `ticker` → `ZodError` | client.test.ts › every method (one test per method; `market_missing_ticker.json` fails with path `market.ticker`) | PASS |
| 3 | Candlesticks URL and parsed candle | client.test.ts › getCandlesticks (`?start_ts=…&end_ts=…&period_interval=1`; second candle has `trade_close_bp: null`) | PASS |
| 4 | Kill switch → `NetworkPaused` for every method, zero requests | client.test.ts › network gate (list of calls checked against the class's public methods; msw `request:start` count 0) | PASS |
| 5 | Rate limiter and backoff (fake timers) | `test/unit/kalshi/rateLimiter.test.ts` (60 at t0, then one every 50 ms; sends at 0 / 500 / 1500 ms; five 503 attempts at 0 / 500 / 1500 / 3500 / 7500 ms) | PASS |
| 6 | Logs never contain the key id or a `/portfolio/*` body | client.test.ts › logging (one debug-level logger for the whole file; key id, balances, order ids, fees and error messages absent; no query strings) | PASS |
| 7 | Discovery (2 games, preseason skipped at info, 2 + 3 markets, idempotent, include_preseason) | `test/unit/kalshi/discovery.test.ts` (NHL events span two pages) | PASS |
| 8 | Team mapping (`-CFC`, `TIE`, unknown target → one warn) | discovery.test.ts › team mapping | PASS |
| 9 | `createOrderV2` body, endpoint, fill and fee = `feeMicros` | client.test.ts › createOrderV2; `test/unit/pricing.test.ts` (§2 examples) | PASS |
| 10 | `getOrders` sends `ticker`, `min_ts`, never `client_order_id`; paginates | client.test.ts › getOrders | PASS |
| 11 | `kalshi:smoke` with a demo key / without a key | without a key: `SKIPPED (no demo key)`, exit 0. Against the fixture stand-in (`KALSHI_SCRIPT_BASE_URL`): environment, balance, exchange status, events per series. **Against demo: not run** — no demo key and no route to Kalshi from this session (a signed request is answered `403` by the egress proxy) | PARTIAL — box left unticked |
| 12 | e2e Settings → Leagues and Test Kalshi connection | `test/e2e/leagues.spec.ts` at 1280 px and 390 px (six leagues with series; Discover series lists the six `…GAME` series and not `KXNHLTOTAL`; include-preseason toggled, reloaded, checked in the DB, restored; Run discovery now; `demo` and `$123.45`) | PASS |

`npm run lint`, `npm run typecheck`, `npm test` (262 passed, 1 skipped as root: the T02 `chmod 555` test),
`npm run e2e` (22 passed) and `npm audit --audit-level=high` (exit 0) pass.

## `npm run verify:T06` output

```
PASS   Signing: fixed timestamp/GET/path signs "1700000000000GET/trade-api/v2/portfolio/balance", verifies; query excluded — 4 tests passed, 22 skipped
PASS   Every method has an msw test returning typed integers; NO bid 0.0700×50.00 → ask {9300, 5000}; unknown field passes; missing ticker → ZodError — 20 tests passed, 6 skipped
PASS   Candlesticks URL /series/KXNHLGAME/markets/<ticker>/candlesticks?…&period_interval=1; ask_close_bp, bid_close_bp, nullable trade_close_bp — 1 tests passed, 25 skipped
PASS   Network gate: global_kill_switch=true → every method rejects NetworkPaused, msw records zero requests — 3 tests passed, 29 skipped
PASS   Rate limiter (fake timers): 600 tokens after 3 s; 40 + 40 → 60 at once then 20/s; 429,429,200 after 0.5 s + 1 s; six 503 → KalshiUnavailable after 5 — 4 tests passed
PASS   Logs from the whole client run never contain the key id or any /portfolio/* response body — 26 tests passed
PASS   Discovery: 2 games (preseason skipped, info), 2 + 3 markets with price_ranges; second run only updated_at; include_preseason adds it — 3 tests passed, 7 skipped
PASS   Team mapping: -CFC via custom_strike to the matching team; TIE → tie; unknown target → unknown + one warn, continues — 3 tests passed, 7 skipped
PASS   createOrderV2 body to POST /portfolio/events/orders; fill "2.00" → 200 cc, "0.9300" → 9300, fee "0.0046"×2 → 9200 = feeMicros — 8 tests passed, 24 skipped
PASS   getOrders sends ticker and min_ts (never client_order_id) and paginates — 1 tests passed, 25 skipped
PASS   (extra) Leagues / discovery / connection-test API and the daily 05:00 schedule — 8 tests passed, 8 skipped
PASS   kalshi:smoke without a key prints "SKIPPED (no demo key)" and exits 0 — SKIPPED (no demo key)
PASS   kalshi:smoke output format against the fixture stand-in: environment, balance, exchange status, events per enabled series — environment: demo (http://127.0.0.1:8295/trade-api/v2) | subaccount: 0 | balance: $123.450000 (portfolio value $1.860000) | exchange: exchange_active=true trading_active=true | open events per enabled series: |   bundesliga KXBUNDESLIGAGAME   0 |   epl        KXEPLGAME          1 |   laliga     KXLALIGAGAME       0 |   ligue1     KXLIGUE1GAME       0 |   nhl        KXNHLGAME          2 |   seriea     KXSERIEAGAME       0 | OK
MANUAL kalshi:smoke with a demo key — no demo key in config.local.json — see docs/verification/T06.md
PASS   e2e: Settings → Leagues six leagues + series; Discover series (mocked); include-preseason persists; Test Kalshi connection shows demo + balance — 4 e2e tests passed (desktop + mobile)
15 of 15 items not failing.
```

## Manual step still open

With a Kalshi **demo** key:

```bash
cd kalshi-trader/app
cat > config.local.json <<'JSON'
{ "kalshiEnv": "demo", "kalshiKeyId": "<key id>", "kalshiPrivateKeyPath": "/path/to/demo-key.pem" }
JSON
npm run kalshi:smoke              # environment, balance, exchange status, open events per enabled series
npm run fixtures:record:kalshi    # raw responses into test/fixtures/kalshi/recorded/ — compare with the hand-written fixtures
```

Things to compare in the recorded payloads (SPEC.md §14 T06 implementation notes): the milestone `details`
home/away team-id keys, the batch live-data path and response key, the order-group and tier-upgrade paths.
