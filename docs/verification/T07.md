# T07 verification — Score feeds, GameTracker, Scheduler, replay, live cards, timeline archive

Environment: Linux x86_64 (running as root), Node.js v22.22.2, npm 10.9.7, msw 2.15, Playwright 1.63 with the
pre-installed Chromium, 2026-09-26. The session's egress policy blocks every Kalshi host and `api-web.nhle.com`
(the proxy answers `403`), so every check runs on fixtures: `test/fixtures/kalshi/`, `test/fixtures/nhl/` and
`test/fixtures/replay/nhl-sample.jsonl`, served by `msw` (unit tests) or `test/e2e/fake-kalshi.ts` (e2e, smoke
format; NHL under `/nhl/v1`).

Automated checks: `cd kalshi-trader/app && npm run verify:T07`.

## Results

| # | Acceptance item | How | Result |
| --- | --- | --- | --- |
| 1 | Soccer minute parser table; unseen text → derived minute + one `warn` per game id | `test/unit/feeds/soccerMinute.test.ts` › parser (table of `parseSoccerLiveText` plus the same texts through `liveDataToState`; two games × three unknown observations → exactly two warns, one per game id) | PASS |
| 2 | Derived minute with fake timers (30 at T+30:10, 65 at S+20, caps 45 / 90) | soccerMinute.test.ts › derived soccer minute (`vi.useFakeTimers`, `derivedSoccerMinute` and the adapter) | PASS |
| 3 | Kalshi live fixture: round 2 `12:34` → live, P2, 754 s, minute 27; round 1 `00:00` → intermission; `finished` → `regulationOver` | `test/unit/feeds/kalshiLive.test.ts` › GameState (also round 2 `00:00`, round 3 `00:00`, overtime, `none`, and the recorded `live_data.json`) | PASS |
| 4 | NHL adapter: `LIVE` P3 `05:00` → 55; `OFF`/`FINAL` → finished; `FUT` → scheduled; `CRIT` → live | `test/unit/feeds/nhl.test.ts` (plus matching by tricodes / start time / source id, `score/now` + `landing` via msw, the network gate) | PASS |
| 5 | Batch: 8 live milestones → exactly one live-data request per tick | kalshiLive.test.ts › batch (msw count after 1, 2, 3 ticks; the request carries all 8 milestone ids) | PASS |
| 6 | Tracker replay of `nhl-sample.jsonl`: snapshot sequence, phases, one snapshot row per line, `hist_games` (`source='live'`) with the goal events, `timeline_archived=1` | `test/unit/core/tracker.test.ts` › replay (`toMatchSnapshot`, `__snapshots__/tracker.test.ts.snap`; goals `home P1 11:45`, `away P2 27:26`, `home P3 55:00`) | PASS |
| 7 | Disagreement: 2-1 vs 1-1 for 25 s → `blocked=1` + `warn`; agreement → `blocked=0`; `stateUpdated` carries `blocked` | tracker.test.ts › feed disagreement (observations every 5 s; blocked from the 25 s observation on; one warn naming both scores) | PASS |
| 8 | Scheduler cadence (fake timers), kill switch → paused with zero requests and `/healthz` `200 {"ok":true,"loop":"paused"}`, resume within one interval, stopped → `503` after 2 min | `test/unit/core/scheduler.test.ts` (fake `setTimeout`/`Date`; `/healthz` through `app.inject`; exact body strings) | PASS |
| 9 | A feed throwing on every call does not stop the other feed or the loop; the status strip shows it as `error` | scheduler.test.ts › throwing (6 + 6 polls in 30 s, snapshots from the healthy feed, status `error` with the message, then recovery); `test/unit/web/StatusStrip.test.tsx` (the strip renders "NHL official API: error", "1 failing") | PASS |
| 10 | `npm run replay … --speed 100` against the dev server: card appears, score changes, "Final" within 30 s; SSE frames include `games[]` with `homeScore`, `awayScore`, `clock` (incl. `minuteSource`) | `verify:T07` starts `npm run dev` on :8397 (fresh DB), signs in with headless Chromium, records every SSE `games` frame in the page, runs `npm run replay`; card after 0.8 s, "Final" after 9.9 s. Also `test/e2e/replay.spec.ts` (production build, 1280 px and 390 px) and `test/unit/feeds/routes.test.ts` › SSE games | PASS |
| 11 | `npm run feeds:smoke` prints today's NHL games from the real NHL API or `no games today`; exits 0 | **Not reachable from this session** (`FAIL NHL API answered 403 for GET /score/now` — the egress proxy). Output format checked against the fixture stand-in (`NHL_SCRIPT_BASE_URL`): `3 NHL game(s) today` + one line per game, exit 0. CI job `feeds-smoke` runs it against the real API | PENDING CI — box left unticked |
| 12 | e2e: Settings → Feeds toggles persist; "Test feed" shows one result line per adapter | `test/e2e/feeds.spec.ts` at 1280 px and 390 px (NHL switch off → reload → DB `{"nhl-official":false}` → on again; two result lines: Kalshi `OK`, NHL `OK — 3 NHL game(s) today`) | PASS |

`npm run lint`, `npm run typecheck`, `npm test` (312 passed, 1 skipped as root: the T02 `chmod 555` test),
`npm run e2e` (26 passed) and `npm audit --audit-level=high` (exit 0; 4 pre-existing moderate advisories in
`drizzle-kit`'s dev tooling) pass.

## `npm run verify:T07` output

```
PASS   Soccer minute parser table: 78' → 78, 45+2' → 45, 90+4' → 90, HT, FT, 1st Half (no minute), Postponed; unseen → derived + one warn per game — 12 tests passed, 2 skipped
PASS   Derived minute (fake timers): T+30 min 10 s → 30; S+20 min → 65; capped at 45 / 90 — 2 tests passed, 12 skipped
PASS   Kalshi live fixture (NHL): round 2 "12:34" → live, period 2, 754 s, minute 27; round 1 "00:00" → intermission; finished → regulationOver — 4 tests passed, 2 skipped
PASS   NHL adapter: LIVE P3 "05:00" → 55; OFF/FINAL → finished; FUT → scheduled; CRIT → live — 6 tests passed
PASS   Batch: 8 live milestones → exactly one live-data request per tick (msw count) — 2 tests passed, 4 skipped
PASS   Tracker replay of nhl-sample.jsonl: toMatchSnapshot; scheduled → live → intermission → live → finished; one snapshot per line; hist_games live + goals; timeline_archived=1 — 1 tests passed, 4 skipped
PASS   Disagreement: 2-1 vs 1-1 for 25 s → blocked=1 + warn; agreement → blocked=0; stateUpdated carries blocked — 2 tests passed, 3 skipped
PASS   Scheduler (fake timers): idle 0 calls / 10 min; pre-game every 60 s; live every 5 s; finished → idle; kill switch 0 requests + /healthz 200 paused, resumes; stopped → 503 stale after 2 min — 10 tests passed
PASS   A feed throwing on every call does not stop the other feed or the loop; the status strip shows it as error — 1 tests passed, 9 skipped; status strip: 1 tests passed, 3 skipped
PASS   (extra) Feeds API (toggle + audit, Test feed), /api/dev/replay access, SSE games frames — 5 tests passed
PASS   npm run replay -- --file test/fixtures/replay/nhl-sample.jsonl --speed 100 against the dev server: card appears, score changes, "Final" within 30 s; SSE games[] with homeScore, awayScore, clock (incl. minuteSource) — card after 0.8 s, "Final" after 9.9 s; 11 SSE games[] frames for the game, e.g. {"id":"KXNHLGAME-26OCT14SEAVGK","leagueId":"nhl","sport":"hockey","competition":"Pro Hockey","homeTeam":"Vegas Golden Knights","awayTeam":"Seattle Kraken","homeAbbr":"VGK","awayAbbr":"SEA","homeScore":0,"awayScore":0,"phase":"live","clock":{"regulationOver":false,"period":1,"secondsLeftInPeriod":1200,"minute":0,"minuteSource":"feed"},"blocked":false,"scheduledAt":"2026-09-26T02:38:25.883Z","observedAt":"2026-09-26T02:38:26.801Z","source":"kalshi-live","strategies":[]}
PASS   (extra) dev server logged no error lines during the replay — 0 error lines
MANUAL npm run feeds:smoke against the real NHL API — api-web.nhle.com is not reachable from here (FAIL NHL API answered 403 for GET /score/now); CI runs it — see docs/verification/T07.md
PASS   feeds:smoke output format against the fixture stand-in (3 games: LIVE with score and clock, FUT, OFF); exit 0 — 3 NHL game(s) today: |   2026020045  UTA @ VGK  LIVE 1-2 P3 05:00  2026-10-11T02:00:00Z |   2026020046  EDM @ SEA  FUT  2026-10-11T02:30:00Z |   2026020040  BOS @ TOR  OFF 3-4 P3 00:00  2026-10-10T23:00:00Z
PASS   e2e: Settings → Feeds toggles persist; "Test feed" shows one result line per adapter; replay spec on the production build — 4 e2e tests passed (desktop + mobile)
15 of 15 items not failing.
```

## Manual step still open

Run `npm run feeds:smoke` with access to `api-web.nhle.com` (any machine, or the CI job `feeds-smoke` in
`.github/workflows/ci.yml`): it prints `N NHL game(s) today:` followed by one line per game, or `no games today`,
and exits 0.
