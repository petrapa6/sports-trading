# T10 verification — Stats endpoint and Recharts dashboard, split by mode

Environment: Linux x86_64 (running as root), Node.js v22, npm 10, Playwright 1.63 with the pre-installed
Chromium, 2026-09-26. Everything runs locally; no Kalshi or NHL host is contacted.

Automated checks: `cd kalshi-trader/app && npm run verify:T10`.

## Results

| # | Acceptance item | How | Result |
| --- | --- | --- | --- |
| 1 | `stats-seed.sql` → `/api/stats` equals `stats-seed.expected.json` for unfiltered, `strategies=A`, `leagues=epl`, `mode=dry_run`, `mode=live`, a range covering half the trades (`range=7d` with the clock at 2026-09-20 12:00); win rate / ROI / max drawdown rules | `test/unit/stats/stats.test.ts` (6 cases compared field by field, plus explicit tile checks); hand computation in `test/fixtures/db/stats-seed.md` | PASS |
| 2 | Mode separation: one live trade changed → only `live` changes; nothing sums both modes | stats.test.ts › mode separation (combined response equals the per-mode responses; after changing #16 `dry_run` is identical; no tile equals live + dry run) | PASS |
| 3 | Equity ordered by `settled_at`; one bankroll point per `bankroll_snapshots` row, one balance point per `balance_snapshots` row in range | stats.test.ts › equity points (all dates and `range=7d`, counts from SQL) | PASS |
| 4 | Implied vs actual: one point per (strategy, league) per mode with `x`, `y`, `n` | stats.test.ts › implied vs actual | PASS |
| 5 | Empty DB → `200`, zeroed tiles, empty arrays for both modes; unknown league → `400` | stats.test.ts › edge cases | PASS |
| 6 | 500 seeded trades → < 200 KB, no `trigger_snapshot` | stats.test.ts › 500 seeded trades (`seedDemo`, the `npm run seed:demo` generator) | PASS |
| 7 | 10 000 trades → median < 300 ms over 20 calls | `test/unit/stats/perf.test.ts`: median 110.7 ms, min 92.2 ms, max 144.6 ms (response 1.9 MB) | PASS |
| 8 | e2e on the seeded DB: eight `.recharts-wrapper`; mode = both → every legend has "Live" and "Dry run", every tile two values; mode = live → no "Dry run" in any chart; empty DB → empty states; no overflow at 390 px; dark mode changes series colours | `test/e2e/dashboard.spec.ts` at 1280 px and 390 px (no CSP violations) | PASS |
| 9 | Trades page histogram and per-trade bars follow the filter; hover shows trade id and mode | dashboard.spec.ts (bar count = settled rows for three filters; histogram bars ≤ filled rows; hover tooltip `Trade stats-tNN` + mode) | PASS |

`npm run lint`, `npm run typecheck`, `npm test` (456 passed, 1 skipped as root: the T02 `chmod 555` test),
`npm run e2e` (32 passed) and `npm audit --audit-level=high` (exit 0) pass. `npm run seed:demo -- --trades 500`
against a scratch database: `{"strategies":3,"trades":500,"attempts":1012,"bankrollSnapshots":484,"balanceSnapshots":121}`.

`docker compose up --build` could not be run in this session (no Docker daemon available); the `Image` workflow
covers it on the pushed branch.

## `npm run verify:T10` output

```
PASS   stats-seed.sql → GET /api/stats equals stats-seed.expected.json (…) — 8 tests passed, 7 skipped
PASS   Mode separation … — 1 tests passed, 14 skipped
PASS   Equity ordered by settled_at; one bankroll / balance point per snapshot row in range — 1 tests passed, 14 skipped
PASS   Implied vs actual … — 1 tests passed, 14 skipped
PASS   Empty DB → 200 with zeroed tiles and empty arrays; unknown league → 400 — 2 tests passed, 13 skipped
PASS   500 seeded trades → response < 200 KB, no per-trade rows — 1 tests passed, 14 skipped
PASS   10 000 trades → /api/stats median < 300 ms over 20 calls — 10000 trades, 20 calls: median 110.7 ms, min 92.2 ms, max 144.6 ms, response 1892573 bytes
PASS   e2e: eight charts … — 2 e2e tests passed (1280 px + 390 px)

8 of 8 items not failing.
```
