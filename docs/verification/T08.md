# T08 verification — Strategy model, effective mode, engine, Strategies page

Environment: Linux x86_64 (running as root), Node.js v22.22.2, npm 10.9.7, Playwright 1.63 with the pre-installed
Chromium, 2026-09-26. Everything runs locally on fixtures: engine tests build `GameState`s through the real Kalshi
live-data adapter (`liveDataToState`), the replay uses `test/fixtures/replay/nhl-sample.jsonl`, and no Kalshi or NHL
host is contacted.

Automated checks: `cd kalshi-trader/app && npm run verify:T08`.

## Results

| # | Acceptance item | How | Result |
| --- | --- | --- | --- |
| 1 | `modes.ts` table test over all 32 combinations of the five switches | `test/unit/core/modes.test.ts`: every combination (checked to be 32 distinct ones) against an independent transcription of §1; distribution 16 / 8 / 4 / 2 / 1 / 1 | PASS |
| 2 | Soccer rule `{minLead:2, atMinute:80, windowMinutes:5, leaderSide:'any'}` table | `test/unit/core/engine.test.ts` › soccer: states from Kalshi `widgetLiveText` (`80'`, `79'`, `84'`, `86'`, `HT`, `FT`, `90+3'`); home / away market tickers; `blocked` → exactly one `debug` line with `mode`; `90+3'` → minute 90 → no signal with window 80–85, a signal with 85–90 | PASS |
| 3 | Hockey `{minLead:2, atMinute:50}`: P3 10:00 3-1 → signal; 10:01 → none; P2, intermission, OT → none | engine.test.ts › hockey (states from Kalshi `round` / `final_round_time_left`; OT also with a state claiming minute 50) | PASS |
| 4 | Once per game: a `trades` row → nothing; another strategy on the same game still signals | engine.test.ts › once per game (plus: at most one signal per (strategy, game) per process; a replay reset forgets it) | PASS |
| 5 | Paused strategies are not evaluated (spy): strategy kill switch → zero evaluations; global kill switch → the engine receives no states | engine.test.ts › paused (spy on the evaluator); `test/unit/strategies/pipeline.test.ts` › global kill switch (real `Scheduler` + tracker + engine, fake timers: 10 min with the switch on → 0 feed calls, `onState` never called; off → a state and one signal within 5 s) | PASS |
| 6 | Signal labelling: `allowLiveOrders=false` + strategy `live` → `configuredMode:'live', effectiveMode:'dry_run', modeReason:'addon_lock'`; log line `"mode":"dry_run"` | engine.test.ts › signal labelling (also `global_dry_run`, `strategy`, `live/null`) | PASS |
| 7 | Validation: `percent: 150` → 400 naming `sizing.percent`; soccer `atMinute: 95`; `minLead: 0`; `minPrice ≥ maxPrice`; `leagueIds: ['nba']`; hockey with EPL → 400 | `test/unit/strategies/routes.test.ts` › validation (through `app.inject`, each asserting the issue path and that nothing was written; plus hockey `atMinute: 60`, 5-decimal price, unknown field, create in live / kill switch off, the same checks on edit) | PASS |
| 8 | API: create; edit `minLead` → v2, v1 unchanged; `trades` on v1 joins to its rule; toggles create no version; delete hidden unless `?includeDeleted=1` | routes.test.ts › strategy API (SQL join `trades` ⋈ `strategy_versions` → `minLead` 2 after the edit to 3; rename alone → no version; sport change → 400) | PASS |
| 9 | Step-up: kill switch off / mode → live without recent re-auth → `403 reauth_required`; with it → 200 + audit row; kill switch on / mode → dry run need none | routes.test.ts › step-up (session `last_auth_at` aged 10 min; `POST /auth/reauth`; audit rows with `mode`, actor, `{from,to}`) | PASS |
| 10 | Replay of `nhl-sample.jsonl` with a matching strategy (kill switch off) → exactly one `signal` SSE event with `snapshot.homeScore`, `snapshot.clock.minute`, `marketTicker`, `effectiveMode` | pipeline.test.ts › replay (listening app, `POST /api/dev/replay`, SSE reader) and `verify:T08` against a fresh `npm run dev` with `npm run replay --speed 100`: strategy `{minLead:1, atMinute:55, windowMinutes:5}` matches on two lines (55 and 59) → one signal, `marketTicker` `KXNHLGAME-26OCT14SEAVGK-VGK`, 2-1 at minute 55, `dry_run` / `addon_lock` | PASS |
| 11 | e2e: create (invalid percent → inline error first), kill switch off (password prompt), edit → two versions; mode → live → re-auth prompt, badge `LIVE → DRY RUN (add-on lock)` | `test/e2e/strategies.spec.ts` at 1280 px and 390 px (session aged before each step-up so the prompt must appear; DB and audit checked; no CSP violations, no sideways scroll) | PASS |

`npm run lint`, `npm run typecheck`, `npm test` (392 passed, 1 skipped as root: the T02 `chmod 555` test),
`npm run e2e` (28 passed) and `npm audit --audit-level=high` (exit 0; the 4 pre-existing moderate advisories in
`drizzle-kit`'s dev tooling) pass.

`docker compose up --build` could not be run in this session: the Docker daemon starts, but the egress proxy
refuses the blob download of the pinned `ghcr.io/home-assistant/base` image (`403 Forbidden` from
`pkg-containers.githubusercontent.com`). The `Image` workflow (amd64 compose + arm64 build) covers it on the pushed
branch.

## `npm run verify:T08` output

```
PASS   modes.ts table test over all 32 combinations of the five switches (global kill → paused; strategy kill → paused; add-on lock; global dry run; strategy; live) — 35 tests passed
PASS   Soccer {minLead:2, atMinute:80, windowMinutes:5, any}: 2-0@80 home; 79 none; first seen 84 signal; 86 none; 1-0 none; 0-2 away; leaderSide home + 0-2 none; halftime / regulationOver / blocked (debug line) none; 90+3 = 90 only with a window covering 90 — 11 tests passed, 13 skipped
PASS   Hockey {minLead:2, atMinute:50}: P3 10:00 3-1 → signal; 10:01 none; P2 none; intermission none; OT none — 6 tests passed, 18 skipped
PASS   Once per game: a trades row for (strategy, game) → re-evaluation emits nothing; another strategy on the same game still signals — 3 tests passed, 21 skipped
PASS   Paused strategies are not evaluated (spy): strategy kill switch on → zero evaluations; global kill switch on → the engine receives no states at all — 2 tests passed, 22 skipped; scheduler + engine: 1 tests passed, 1 skipped
PASS   Signal labelling: allowLiveOrders=false + strategy live → configuredMode live, effectiveMode dry_run, modeReason addon_lock; log line "mode":"dry_run" — 2 tests passed, 22 skipped
PASS   Validation: percent 150 → 400 naming sizing.percent; soccer atMinute 95; minLead 0; minPrice ≥ maxPrice; leagueIds ['nba']; hockey + EPL → 400 — 13 tests passed, 6 skipped
PASS   API: create → kill_switch=1, dry_run, current_version=1, one version; edit minLead → v2 with v1 unchanged; trades on v1 join to its rule; toggles create no version; delete hidden unless ?includeDeleted=1 — 4 tests passed, 15 skipped
PASS   Step-up: kill switch off / mode → live without recent re-auth → 403 reauth_required; with it → 200 + audit row; kill switch on / mode → dry run need none — 2 tests passed, 17 skipped
PASS   Replay (nhl-sample.jsonl) through the app with a matching strategy (kill switch off) → exactly one SSE signal with snapshot.homeScore, snapshot.clock.minute, marketTicker, effectiveMode — 1 tests passed, 1 skipped
PASS   npm run replay -- --file test/fixtures/replay/nhl-sample.jsonl --speed 100 against npm run dev with a matching strategy (kill switch off) → exactly one SSE signal event for the game — 1 signal: {"strategyId":"28f0b59e-8ddc-4b40-b1ca-7d311d5fcae6","marketTicker":"KXNHLGAME-26OCT14SEAVGK-VGK","effectiveMode":"dry_run","modeReason":"addon_lock","homeScore":2,"awayScore":1,"minute":55}
PASS   (extra) dev server logged the signal with its mode — {"level":30,"time":"2026-09-26T04:33:04.594Z","app":"kalshi-trader","component":"engine","mode":"dry_run","configuredMode":"dry_run","modeReason":"addon_lock","strategyId":"28f0b59e-8ddc-4b40-b1ca-7d311d5fcae6","strategyVersion":1,"gameId":"KXNHLGAME-26OCT14SEAVGK","marketTicker":"KXNHLGAME-26OCT14SEAVGK-VGK","score":"2-1","minute":55,"msg":"Signal: \"NHL lead at 55\" v1 on KXNHLGAME-26OCT14SEAVGK
PASS   (extra) dev server logged no error lines — 0 error lines
PASS   e2e: create (invalid percent → inline error first), kill switch off (password prompt), edit → two versions; mode → live opens the re-auth prompt and the badge reads LIVE → DRY RUN (add-on lock) — 2 e2e tests passed (1280 px + 390 px)
14 of 14 items not failing.
```
