# T12 verification — Backtest simulator and Backtest page

Environment: Linux x86_64 (running as root), Node.js v22.22, npm 10, Playwright 1.63 with the pre-installed
Chromium, 2026-09-26. Everything runs locally; no Kalshi or NHL host is contacted.

Automated checks: `cd kalshi-trader/app && npm run verify:T12`.

Re-verified after T11 was merged underneath T12 (the simulator now uses T11's price model and clock model; see
SPEC.md §14 T12 implementation notes). Results below are from that run.

## Results

| # | Acceptance item | How | Result |
| --- | --- | --- | --- |
| 1 | Determinism: same backtest twice → byte-identical `backtest_trades` (excluding ids) | `test/unit/backtest/routes.test.ts` › determinism (two runs of 300 games through the API and the worker, rows compared as JSON without `id` / `backtest_id`); simulator.test.ts › same input twice | PASS |
| 2 | Parity: `test/fixtures/parity/game-a.json` (goals 12', 55', 78') through GameTracker → StrategyEngine → Executor (dry run) → Settler with the orderbook mocked from the candle series, and through the simulator in exact mode (on the timeline the tracker archived and the same candles in `hist_prices`) → same entry minute (80), limit price ($0.89), contracts, fee and P&L | `test/unit/backtest/parity.test.ts` › game-a | PASS |
| 3 | Exact mode: candle at 80 → `candle`; absent at 80, present at 82 → `next_candle`; none within 3 min → `skipped_no_price`; ask close (not trade close) used | simulator.test.ts › exact mode (incl. the database loader test with trade close ≠ ask close and a `NULL` trade close) | PASS |
| 4 | Retry parity: ask above `maxPrice` at 80' (and 81'), below at 82' → entry at 82' in both | parity.test.ts › retry parity (live attempts: `price`, `price`, fill); simulator.test.ts › retry | PASS |
| 5 | Modelled: every trade `price_source='model'`; summary `priceMode='modelled'`, `minSampleSize` | simulator.test.ts › modelled mode | PASS |
| 6 | Compounding: $100, 2 %, two winning trades → second stake is 2 % of the post-first-trade bankroll (micros) | simulator.test.ts › bankroll $100 | PASS |
| 7 | Settlement: soccer 2-1 → won; NHL 2-2 after regulation, 3-2 in OT for the leader → won; lead lost in OT → lost; NHL tie → void at $0.50 | simulator.test.ts › settlement | PASS |
| 8 | Worker isolation: 5 000-game backtest, `/healthz` < 100 ms, SSE progress increasing | routes.test.ts › worker isolation: 139 `/healthz` calls over real HTTP during the run, max 6.4 ms; 51 `backtest` progress events, `done` strictly increasing to 5000, `total` 5000 | PASS |
| 9 | Performance: 1 300 games < 5 s | routes.test.ts › performance: 1 300 games in exact mode with candles (434 trades) in 1 408 ms, from `POST` to the worker's result (worker start included) | PASS |
| 10 | Promote → strategy `mode='dry_run'`, `kill_switch=1`, version 1 = backtest params | routes.test.ts › promote | PASS |
| 11 | e2e: form run → tiles, equity / drawdown / monthly charts, trades table; save; second run with another `atMinute`; comparison shows two equity lines; modelled badge only in modelled mode; no "Live" / "Dry run" legend | `test/e2e/backtest.spec.ts` at 1280 px and 390 px (also the Strategies page "Test against last 30 days", no horizontal overflow, no CSP violations) | PASS |

`npm run lint`, `npm run typecheck`, `npm test` (513 passed, 1 skipped as root: the T02 `chmod 555` test),
`npm run e2e` (36 passed, T11's `data.spec.ts` included), `npm run verify:T11` (9 of 9) and `npm audit --audit-level=high` (exit 0) pass. The compiled worker
(`dist/backtest/worker.js`, used in the image) was run once from `npm run build` output against a scratch database:
50 games, `status: done`.

`docker compose up --build` could not be run in this session (no Docker daemon available); the `Image` workflow
covers it on the pushed branch.

## Manual check

The Backtest page was screenshotted at 1280 px after the e2e run (two saved runs, comparison open): form, modelled
badge, nine tiles, three charts, trades table, saved list and the two-line comparison render without overlap.

## `npm run verify:T12` output

```
PASS   Determinism: the same backtest twice → byte-identical backtest_trades (excluding ids) — 4 tests passed, 16 skipped
PASS   Parity: game-a.json through tracker → engine → executor (dry run) and the simulator (exact) → same minute, limit price, contracts, fee, P&L — 1 tests passed, 1 skipped
PASS   Exact mode: candle / next_candle / skipped_no_price; ask close, not trade close — 2 tests passed, 13 skipped
PASS   Retry parity: ask above maxPrice at 80 and below at 82 → entry at 82 in simulator and live replay — 2 tests passed, 15 skipped
PASS   Modelled mode: price_source=model; priceMode=modelled with minSampleSize — 2 tests passed, 13 skipped
PASS   Compounding: second stake is 2 % of the post-first-trade bankroll — 1 tests passed, 14 skipped
PASS   Settlement: soccer 2-1 won; NHL OT win won; OT loss lost; NHL tie void at $0.50 — 2 tests passed, 13 skipped
PASS   Worker isolation: 5 000 games, /healthz < 100 ms, SSE progress increasing — 155 /healthz calls during a 5000-game backtest: max 5.2 ms; 51 progress events
PASS   Performance: 1 300 games in < 5 s — 1300 games (exact, 434 trades) in 1492 ms
PASS   Promote: strategy with mode dry_run, kill_switch 1, version 1 = backtest params — 1 tests passed, 4 skipped
PASS   e2e: run from the form (tiles, equity/drawdown/monthly charts, trades table), save, second run with another atMinute, comparison with two equity lines, modelled badge only in modelled mode, no Live / Dry run legend — 2 e2e tests passed (1280 px + 390 px)

11 of 11 items not failing.
```
