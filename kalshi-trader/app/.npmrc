# Keep npm's own banner ("> kalshi-trader@0.1.0 dev") off stdout so `npm run dev` prints only
# the app's JSON log lines (T01 acceptance). Pass --loglevel=warn to see npm diagnostics.
loglevel=silent
